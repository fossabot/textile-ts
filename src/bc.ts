import type { JsonMLRoot,JsonMLNode } from "./lib/shares/types.js";

const isCodeBlock = (node:JsonMLNode)=>{
    
}
const getLangCode = (node:JsonMLNode)=>{
    if(typeof node === "string"){
        return undefined
    }

}
